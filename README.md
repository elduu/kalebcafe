# InviteYours — Pure React + TypeScript + Tailwind

Plain Vite + React 19 + TypeScript + Tailwind CSS v4. No router, no SSR.

## Run
```
npm install
npm run dev
```
Open http://localhost:5173

## Build
```
npm run build && npm run preview
```
